// Standalone YouTube Intelligence page — no sidebar, no account info.
// Re-exports the full page component from the main app so logic stays in one place.
export { default } from '@/app/(main)/youtube-intel/page';
